import React from "react";
import "../WalletPage_txt/Style.css";
const WalletPage_txt = () => {
  return (
    <div>
      <h1>Wallets</h1>
      <p>
        Choose your wallet from the list below to synchronize it and proceed.
        Multiple iOS and Android wallets support the Wallet Resolver protocol.
        Interaction between mobile apps and mobile browsers is supported via
        mobile deep linking.
      </p>
    </div>
  );
};

export default WalletPage_txt;
